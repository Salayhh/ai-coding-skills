interface:
  display_name: "Architecture Auditor"
  short_description: "Audit architecture without changing code"
  default_prompt: "Use $architecture-auditor to audit this codebase read-only and generate architecture control documents."
